server:
  port: 7004

spring:
  profiles:
    active: dev
  application:
    name: bee-health-member
    admin:
      enabled: true
      jmx-name: org.springframework.boot:type=Admin,name=BeeHealthMemberApplication