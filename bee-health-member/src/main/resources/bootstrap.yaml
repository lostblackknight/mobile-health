server:
  port: 7004

spring:
  application:
    name: bee-health-member
  cloud:
    nacos:
      server-addr: 192.168.86.136:8848
      config:
        file-extension: yaml

management:
  endpoints:
    web:
      exposure:
        include: all
