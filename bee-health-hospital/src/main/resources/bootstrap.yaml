server:
  port: 7003

spring:
  application:
    name: bee-health-hospital
  cloud:
    nacos:
      server-addr: 192.168.86.134:8848
      config:
        file-extension: yaml

management:
  endpoints:
    web:
      exposure:
        include: all
