server:
  port: 7006

spring:
  profiles:
    active: dev
  application:
    name: bee-health-order
    admin:
      enabled: true
      jmx-name: org.springframework.boot:type=Admin,name=BeeHealthOrderApplication