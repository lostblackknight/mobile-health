server:
  port: 7006

spring:
  application:
    name: bee-health-order
  cloud:
    nacos:
      server-addr: 192.168.86.136:8848
      config:
        file-extension: yaml

management:
  endpoints:
    web:
      exposure:
        include: all
