server:
  port: 7007

spring:
  profiles:
    active: dev
  application:
    name: bee-health-search
    admin:
      enabled: true
      jmx-name: org.springframework.boot:type=Admin,name=BeeHealthSearchApplication
