server:
  port: 7005

spring:
  application:
    name: bee-health-message
  cloud:
    nacos:
      server-addr: 192.168.86.136:8848
      config:
        file-extension: yaml

management:
  endpoints:
    web:
      exposure:
        include: all
