server:
  port: 7005

spring:
  application:
    name: bee-health-message
    admin:
      enabled: true
      jmx-name: org.springframework.boot:type=Admin,name=BeeHealthMessageApplication
  cloud:
    nacos:
      server-addr: 192.168.86.137:8848
      config:
        file-extension: yaml

management:
  endpoints:
    web:
      exposure:
        include: all
