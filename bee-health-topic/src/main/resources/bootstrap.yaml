server:
  port: 7009

spring:
  profiles:
    active: dev
  application:
    name: bee-health-topic
    admin:
      enabled: true
      jmx-name: org.springframework.boot:type=Admin,name=BeeHealthTopicApplication